import { StyleSheet } from 'react-native';
import { AppTheme } from '@/themes';

export const getQuestionStyles = (theme: AppTheme) =>
  StyleSheet.create({
    container: {
      width: '100%',
    },
  });
